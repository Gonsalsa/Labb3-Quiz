<UserControl x:Class="Labb3_NET22.PlayQuizView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Labb3_NET22"
        mc:Ignorable="d"
        Height="450" Width="800">
    <Grid Margin="0,4,0,-4" Background="#E6C795">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>


        <TextBlock Text="{Binding Quiz.Title}" FontSize="26"
                   Foreground="#8A72D1"
                   FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0, 0, 0, 10"/>

        <TextBlock Grid.Row="1" Text="{Binding CurrentQuestion.Statement}"
                   FontSize="18" TextWrapping="Wrap"
                   HorizontalAlignment="Center"
                   Margin="0, 10"
                   />

        <StackPanel Grid.Row="2" Margin="0, 20">
            <Button x:Name="Answer1Button" Content="{Binding CurrentQuestion.Answers[0]}"
                    Height="40" Margin="0,5"
                    Click="AnswerButton_Click" Tag="0"/>

            <Button x:Name="Answer2Button" Content="{Binding CurrentQuestion.Answers[1]}"
                    Height="40" Margin="0,5"
                    Click="AnswerButton_Click" Tag="1"/>

            <Button x:Name="Answer3Button" Content="{Binding CurrentQuestion.Answers[2]}"
                    Height="40" Margin="0,5"
                    Click="AnswerButton_Click" Tag="2" />

            <Button x:Name="Answer4Button" Content="{Binding CurrentQuestion.Answers[3]}"
            Height="40" Margin="0,5"
            Click="AnswerButton_Click" Tag="3" />

        </StackPanel>

        <TextBlock Grid.Row="3" Text="{Binding ScoreText}" 
                   FontWeight="Bold" FontSize="18"
                   HorizontalAlignment="Center" Margin="0, 10"/>


    </Grid>
</UserControl>
